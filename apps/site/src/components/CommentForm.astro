---
import PluginCommentForm from '../plugins/comments/CommentForm.astro';

interface Props {
  variant: 'semantic' | 'terminal';
  action: string | null;
  postPath: string;
  parentId?: string;
  consentVersion: string;
}

const props = Astro.props as Props;
---

<PluginCommentForm {...props} />
